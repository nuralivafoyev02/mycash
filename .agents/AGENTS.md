# Project Rules

- **Hover Effects**: Do not use upward translation hover effects (`hover:-translate-y-*` or `active:translate-y-0`) on buttons, cards, or any other elements in the UI. Keep them static vertically on hover.
